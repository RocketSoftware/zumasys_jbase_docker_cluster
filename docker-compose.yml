version: '3'
services:
    db1:
        build: ./db1
        depends_on:
            - db2
        #command: bash -c "rm -f tmp/pids/server.pid && bundle exec rails s -p 3000 -b '0.0.0.0'"
    db2:
        build: ./db2
        #command: bash -c "rm -f tmp/pids/server.pid && bundle exec rails s -p 3000 -b '0.0.0.0'"
    web1:
        build: ./web1
        ports:
            - "80:80"
        depends_on:
            - db1


